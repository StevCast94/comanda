{
  "build": {
    "buildCommand": "cd frontend && npm install && npx vite build --outDir ../backend/public && cd ../backend && npm install && npx prisma generate"
  },
  "deploy": {
    "startCommand": "cd backend && npx prisma migrate deploy && npx tsx src/index.ts"
  }
}
