{
  "build": {
    "buildCommand": "cd backend && npm install && npx prisma generate"
  },
  "deploy": {
    "startCommand": "cd backend && npx prisma migrate deploy && npx tsx scripts/migrate-kitchen.ts && npx tsx src/index.ts",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
